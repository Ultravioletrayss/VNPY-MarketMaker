# Simple Python Demo

This is a tiny Python example project.

Run it with:

```powershell
python main.py
```

It asks for your name, prints a greeting, and calculates the total and average of a few numbers.
